# Examples for package usage in python

<https://www.tutorialsteacher.com/python/python-package>

To run `program.py`

- `pip install --upgrade .` in **src** folder (after each change in sample_project):
- `python .\programm.py` in **src** folder
